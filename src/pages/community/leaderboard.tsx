import React from "react";
import { LeaderboardPage } from "@/modules/community/leaderboard";
import Layout from "@/modules/layout";

const Leaderboard = () => {
  return (
    <Layout>
      <LeaderboardPage />
    </Layout>
  );
};

export default Leaderboard;
